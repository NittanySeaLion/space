<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar">

        <!--
            DreamService — activated automatically when device is charging
            and the screen saver is triggered (Settings > Display > Screen saver).
        -->
        <service
            android:name=".LunarSkyDreamService"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.service.dreams.DreamService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.service.dream"
                android:resource="@xml/dream_info" />
        </service>

        <!-- Settings screen shown when the user taps the gear icon -->
        <activity
            android:name=".DreamSettingsActivity"
            android:label="@string/settings_title"
            android:exported="true" />

    </application>
</manifest>
